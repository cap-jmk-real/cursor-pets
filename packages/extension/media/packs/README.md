Bundled pet packs that ship with the extension.

These packs are copied into the user packs directory on first run:

- Windows: `%USERPROFILE%\.cursor-pets\packs\`
- macOS/Linux: `~/.cursor-pets/packs/`

